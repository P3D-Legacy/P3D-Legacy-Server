name: Publish Docker Image

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        runtime: ['win-x64', 'win-x86', 'win-arm', 'linux-x64', 'linux-arm', 'linux-arm64', 'osx-x64' ]
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Setup .NET 6
      uses: actions/setup-dotnet@master
      with:
        dotnet-version: 6.0.x

    - name: Publish artifacts
      run: dotnet publish src/P3D.Legacy.Server/P3D.Legacy.Server.csproj --configuration Release --framework net6.0 --runtime ${{ matrix.runtime }} --self-contained -p:PublishSingleFile=true -p:PublishTrimmed=true --output ./P3D-Legacy-Server-${{ matrix.runtime }}/*
      shell: pwsh

    - name: Upload artifacts
      uses: actions/upload-artifact@v2
      with:
        name: all-artifacts
        path: |
          release-${{ matrix.runtime }}.zip

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
    - name: Download Artifacts
      uses: actions/download-artifact@v2
      with:
        name: all-artifacts

    - name: Get tag info
      id: tag_info
      run: echo ::set-output name=SOURCE_TAG::${GITHUB_REF#refs/tags/}

    - name: Create release
      uses: softprops/action-gh-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ steps.tag_info.outputs.SOURCE_TAG }}
        name: P3D-Legacy-Server ${{ steps.tag_info.outputs.SOURCE_TAG }}
        draft: false
        prerelease: false
        body: ${{ github.event.head_commit.message }}
        files: |
          P3D-Legacy-Server-*.zip